<%= form_with(model: gm, local: true) do |form| %>
  <% if gm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gm.errors.count, "error") %> prohibited this gm from being saved:</h2>

      <ul>
      <% gm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :gm_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
